{
  "name": "akilipesa-functions",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions --project akilipesa-prod"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1",
    "agora-access-token": "^2.0.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}